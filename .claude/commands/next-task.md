---
description: Find the next unchecked task in docs/build-plan.md
allowed-tools: Read Grep
---

Find the next task to work on:

1. Read `docs/build-plan.md`
2. Find the first unchecked task (lines with `- [ ]`)
3. Note the task number and title
4. Read the spec sections referenced by that task
5. Report: task number, title, spec sections to read, and a brief summary of what the task involves

Do not start implementing — just report what the next task is.
